version: "3"

services:
  srv-notebook:
    build:
      context: .
      dockerfile: services/py/notebook/Dockerfile
    depends_on:
      - consul
      - microhq-sidecar
      - mysql
      - redis
    volumes:
      - ./services/py:/code