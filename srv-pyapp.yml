version: "3"

services:
  srv-pyapp:
    build:
      context: .
      dockerfile: services/py/python_app/Dockerfile
    depends_on:
      - consul
      - microhq-sidecar
      - mysql
      - redis
    volumes:
      - ./services/py:/code